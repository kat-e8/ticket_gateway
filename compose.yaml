services:
  tkt_gw:
    build:
      context: gw-build
      args:
        IGNITION_VERSION: 8.1.48
    hostname: tkt_gw
    container_name: tkt_gw
    ports:
      - 9091:8088
    labels:
      tsdproxy.enable: true
      tsdproxy.ephemeral: false
    restart: unless-stopped
    volumes:
      - tkt-gw-data:/usr/local/bin/ignition/data
    #  - ./gw-init/gateway.gwbk:/restore.gwbk
    #  - ./gw-init/gateway-uuid.txt:/usr/local/bin/ignition/data/.uuid
    #  - ./gw-init/gateway-metro-keystore:/usr/local/bin/ignition/data/local/metro-keystore
    networks:
      - tkt-gw-network
    environment:
      - IGNITION_EDITION=standard
      - GATEWAY_ADMIN_PASSWORD=katlego
      - ACCEPT_IGNITION_EULA=Y
    command: >
      -n gw
      -m 2048
   #   -r /restore.gwbk
networks:
  tkt-gw-network:
volumes:
  tkt-gw-data:

#she's really a mess this one.
